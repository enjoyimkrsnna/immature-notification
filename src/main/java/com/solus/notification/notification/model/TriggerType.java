package com.solus.notification.notification.model;

public enum TriggerType {
    INTERVAL,   // For interval-based notifications
    ONE_TIME,   // For one-time notifications
    ON_APP_OPEN // For app open notifications
}

